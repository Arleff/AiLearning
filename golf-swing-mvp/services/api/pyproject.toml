[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "golf-swing-mvp-api"
version = "0.1.0"
description = "FastAPI backend for golf swing MVP"
requires-python = ">=3.12"
dependencies = ["fastapi", "uvicorn[standard]"]
